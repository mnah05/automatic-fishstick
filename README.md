# automatic-fishstick
fishstick a fault-tolerant distributed job scheduler that reliably dispatches scheduled and background jobs to long-running workers. It provides durable job persistence, worker heartbeats, leases, retries, leader election, and automatic failure recovery while keeping execution logic inside pluggable worker handlers.
